-- This is the main configuration file for AwesomeWM.
-- It is split into sections for easier understanding and maintenance.

-- === ERROR HANDLING ===
-- Check for errors during startup and runtime, and display notifications.

-- === VARIABLE DEFINITIONS ===
-- Configure theme, terminal, editor, and default key bindings (modkey).
-- Define the list of layouts to use (only floating layout enabled here).

-- === MENU ===
-- Create the main application menu and launcher widget.

-- === WIBAR ===
-- Configure the status bar (wibox) for each screen.
-- Sets up wallpaper, tags, prompt box, and (commented out) taglist, tasklist, systray, clock, layout box.

-- === MOUSE BINDINGS ===
-- Define global mouse button actions (e.g., right-click opens main menu).

-- === KEY BINDINGS ===
-- Define global keyboard shortcuts for window management, navigation, etc.
-- Includes standard AwesomeWM keys and custom ones like launching terminal or running prompts.

-- === RULES ===
-- Define rules for how new windows (clients) should behave based on their properties (class, name, etc.).
-- Includes rules for floating windows and general client properties.

-- === SIGNALS ===
-- Define actions to take when specific events happen (e.g., new client, focus change).
-- Includes client management, focus/unfocus color changes, and titlebar setup (commented out).
